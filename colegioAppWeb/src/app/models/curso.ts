export class Curso {
    id: number;
    nombre: string;
    numeroCreditos: number;

    constructor(id:number, nombre:string, numeroCreditos:number, ) { 
        this.id = id; 
        this.nombre = nombre; 
        this.numeroCreditos = numeroCreditos; 
     } 
}